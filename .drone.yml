kind: pipeline
type: docker
name: default
platform:
  os: linux
  arch: amd64
workspace:
  base: /go
  path: src/github.com/unknowntpo/page
steps:
  - name: test
    image: golang:1.20.3
    commands:
      - make test